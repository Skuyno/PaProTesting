target-version = "py314"
extend-exclude = ["migrations"]

[lint]
select = [
    "E", "W",
    "F",
    "I",
    "D",
    "B",
    "UP",
    "ASYNC",
]
[lint.pydocstyle]
convention = "google"

[lint.isort]
known-first-party = ["app"]
